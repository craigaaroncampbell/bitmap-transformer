var expect = require('chai').expect;
var transformer = require(__dirname + '/../bitmap-transformer2.js')

describe('invertNonPal', function(){
  it('should invert the colors of a non-palette bitmap', function(){
    expect(1).to.eql(3)
  })
})


